const BASE_URL = 'http://localhost:7071/api'

const Urls = {
    users: `${BASE_URL}/users`,
    imageUpload: `https://layif3tpw2.execute-api.us-east-2.amazonaws.com/staging/image-upload`
}

export default Urls